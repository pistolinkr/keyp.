/** Fixed strip below the navbar: scroll content passes through with backdrop blur + gradient mask. */
export function NavbarScrollBlur() {
  return <div className="keyp-navbar-blur-mask" aria-hidden />;
}
